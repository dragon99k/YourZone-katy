<header class="page-title">
  <h1>「いいね」をつけたコミュニティ一覧</h1>
</header>

<% if @communities.present? %>
  <div id="search_communities" class="community-item mg-btm-50" data-page="1" data-total="<%= @communities.total_pages %>">
    <%= render :partial => 'communities/community', collection: @communities, as: :community %>

    <div id="loading" class="hidden">
      loading....
    </div>
  </div>
<% else %>
  <div style="margin-top: 40vh; text-align: center;">データがありません。</div>
<% end %>
